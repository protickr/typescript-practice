let point: { x: number; y: number; z?: number } = { x: 10, y: 5, z: 3 };

let coords: { latitude: number; longitude: number } = {
  latitude: 27.17,
  longitude: 100.23,
};

// 1. we can use "," or ";" to separate the object properties when declaring its type
// 2. we can define an optional property of an object by appending "?" after the property name 
